{
  "name": "echelon-foundry-site",
  "private": true,
  "scripts": {
    "build": "node build.js",
    "test": "node --test test/*.test.js",
    "serve": "node build.js && python3 -m http.server 4000 -d dist"
  }
}
